<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Нужно, чтобы предложить пользователю снять ограничения батареи. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            specialUse, а не dataSync: на Android 15 у dataSync-сервисов есть
            суточный лимит в 6 часов, что для постоянно работающего прокси
            неприемлемо. connectedDevice не подходит по смыслу — внешнего
            устройства нет.

            stopWithTask=false: смахивание карточки из «Недавних» не должно
            останавливать прокси. На части прошивок процесс всё равно убивают,
            поэтому дополнительно есть onTaskRemoved + будильник.
        -->
        <service
            android:name=".ProxyService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Локальный MTProto-прокси: держит слушающий сокет и WebSocket-соединения к дата-центрам Telegram, пока пользователь явно не остановит его." />
        </service>

        <!--
            Пустой туннель ради живучести процесса. Трафик не перехватывает:
            маршрут один и ведёт на незанятый адрес собственной подсети.
            Поднимается только с согласия пользователя и только когда в
            системе нет другого активного VPN.
        -->
        <service
            android:name=".TunnelService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".LogActivity"
            android:exported="false"
            android:theme="@style/AppTheme"
            android:label="@string/log_title"
            android:parentActivityName=".MainActivity" />

        <!--
            Плитка в шторке быстрых настроек: короткое нажатие переключает
            прокси, долгое открывает приложение через QS_TILE_PREFERENCES.
        -->
        <service
            android:name=".ProxyTileService"
            android:exported="true"
            android:icon="@drawable/ic_notification"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- Цель долгого нажатия по плитке. -->
        <activity
            android:name=".TilePreferencesActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Сторож: раз в 15 минут проверяет, что прокси жив. -->
        <receiver
            android:name=".WatchdogReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.tgwsproxy.android.WATCHDOG_CHECK" />
            </intent-filter>
        </receiver>

        <!-- Подъём после перезагрузки телефона и после обновления приложения. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
